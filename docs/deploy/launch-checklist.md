# רשימת פעולות לפני עלייה לאוויר

מסמך מצטבר. כל דבר שדורש החלטה או פעולה אנושית נרשם כאן במקום להיזכר בו.
**שום סוד לא נכתב במסמך הזה** — רק מה לעשות ואיפה.

---

## 1. אבטחה — דחוף

- [x] ~~להחליף את סיסמת מסד הנתונים.~~ **בוצע 9.9.2026.**
      הוחלפה ב-`ALTER ROLE` דרך `server/db/rotate-password.ts`, אומתה בחיבור
      אמיתי, ועודכנה ב-`.env.local` וב-Vercel production. הסיסמה שנחשפה כבר
      אינה תקפה.

- [ ] **`AUTH_SECRET` חדש לפרודקשן** — `openssl rand -base64 32`.
      החלפה שלו מנתקת את כל המחוברים, אז עדיף לפני שיש מי להתנתק.
      (קיים ומוגדר; לא הוחלף כי לא נחשף.)

- [ ] לוודא ש-`.env.local` ו-`.backups/` לא נכנסו לגיט. שניהם ב-`.gitignore`,
      וכדאי לוודא שוב לפני הפריסה הראשונה.

## 2. משתני סביבה

- [x] ~~כל המשתנים מ-[vercel-env.md](vercel-env.md) מוגדרים בפרודקשן.~~
- [x] ~~`ANTHROPIC_API_KEY` — הוגדר, ה-AI נבדק ועובד.~~
- [x] ~~`GANTT_INFORU_AUTH` — הוגדר, שליחת מייל נבדקה ועובדת.~~
- [x] ~~`GANTT_AUTH_SEND=true` — קודי כניסה נשלחים באמת.~~
- [ ] **`GANTT_NOTIFICATIONS_SEND` עדיין לא מוגדר, במכוון.** תזכורות
      וסיכומים נכתבים ללוג בלבד. זה המתג האחרון שנשאר.

## 3. מיגרציות

- [x] ~~0007–0011 רצו בפרודקשן. `db:check` מאשר שהמסד תואם לקוד במלואו.~~

לפני כל פריסה שנוגעת בסכימה: `npm run db:migrate` ואז `npm run db:check`.

## 4. Inforu

- [x] ~~מייל נבדק בפרודקשן: קוד כניסה אמיתי ל-tomer@xtra.co.il, Inforu החזיר
      `StatusId: 1`. הלוג רושם `inforu_sent` עם ה-RequestId.~~
- [ ] **SMS עדיין לא נבדק בשליחה אמיתית.** מסלול ה-OTP בטלפון עובד מקומית,
      אבל אף SMS לא יצא בפרודקשן.
- [ ] לבדוק את המכסה בפורטל, ומי מקבל התראה כשהיא נגמרת.
- [ ] `GANTT_SMS_SENDER` — לוודא שהוא ברשימת השולחים המאושרים. שולח לא מאושר
      חוזר כ-HTTP 200 עם `StatusId` שלילי, כלומר "הצליח" שלא הצליח.
- [ ] רק אחרי כל אלה — להחליט על `GANTT_NOTIFICATIONS_SEND=true`.

## 5. הסיכום היומי

- [x] ~~`CRON_SECRET` מוגדר, והג'וב מתוזמן ורץ.~~
- [x] ~~הפעמון פועל: תזכורות נכתבות לפעמון של כל אדם, פעם ביום.~~
- [ ] **המייל עדיין כבוי** — `GANTT_NOTIFICATIONS_SEND` לא מוגדר.
      זה המתג האחרון.

**מגבלת תוכנית:** Vercel Hobby מרשה ריצת cron אחת ביום. הג'וב מתוזמן
ל-06:00 UTC — 09:00 בשעון קיץ, 08:00 בשעון חורף. הכלל הוא "השעה שלו
הגיעה וטרם קיבל היום", אז איחור לא מזיק והקדמה כן; לכן התזמון יושב
אחרי 08:00 בשני צידי המעבר.

כדי לכבד את השעה המדויקת שכל אדם בחר, צריך cron שעתי — כלומר תוכנית Pro.
בלעדיה כולם מקבלים את הסיכום בריצה היומית.

## 6. AI

- [x] ~~Claude מחובר ונבדק בפרודקשן.~~
- [x] ~~הנקודה דורשת התחברות.~~
- [x] ~~מגבלת שימוש יומית במסד: 40 לאדם, 300 לארגון. נספרות לפני הקריאה,
      אז גם קריאה שנכשלה נספרת. צריכת הטוקנים נרשמת לפי מה שהספק דיווח.~~
      לשינוי: `GANTT_AI_DAILY_PER_USER`, `GANTT_AI_DAILY_TOTAL`.
- [ ] **תקרת הוצאה בחשבון Anthropic עצמו** — זה הדבר היחיד שאני לא יכול
      להגדיר. console.anthropic.com → Settings → Limits → Monthly spend limit.
      המגבלות שלמעלה חוסמות בריחה מהמערכת; תקרה בחשבון חוסמת גם מה שלא עובר
      דרכה.

## 7. בדיקות אחרונות

- [ ] כניסה אמיתית של עובד: קוד במייל מגיע, מסך הכניסה לא אומר "נכתב ללוג".
- [ ] `npm run check` ירוק.
- [ ] `npm run db:check` מול פרודקשן.
- [ ] מסך צר על מכשיר אמיתי, לא רק בדפדפן.

---

## מה עוד לא נבדק

- **מייל סיכום אמיתי** — הטקסט וה-HTML נבדקו, אבל אף סיכום לא נשלח לתיבה.
- מסך ההרשאות לא עבר פישוט (נבדק רק כעורך, שאין לו גישה אליו).

## מה כבר בוצע

- **9.9.2026 — נוקו נתוני הדוגמה מ-`neondb`.** 108 רשומות: 67 אירועים,
  37 שורות יומן, 4 לוחות. נשמרו 8 משתמשים, מטריצת ההרשאות והחיבורים.
  גיבוי מלא של כל 14 הטבלאות ב-`.backups/` לפני המחיקה.
- **9.9.2026 — אומת שהמסד אינו משותף.** כל הטבלאות שייכות למערכת הזאת.
- **9.9.2026 — סיסמת המסד הוחלפה** ב-`ALTER ROLE`, אומתה בחיבור אמיתי,
  ועודכנה ב-Vercel וב-`.env.local`. הישנה אינה תקפה.
- **9.9.2026 — הוסרו ספקים שאינם בשימוש** מהקוד ומ-Vercel: SMTP, Gmail,
  Resend, Google OAuth, Gemini.
- **9.9.2026 — נבדק מקצה לקצה בפרודקשן:** כניסה במייל ובנייד, יצירת פרויקט
  ואירוע, שיוך משימה, שינוי מצב, ארכוב והחזרה, הגדרות, ו-AI.
- **9.9.2026 — נמנעה הצפה.** הרצת הג'וב בפרודקשן הראתה שכל 8 העובדים היו
  מקבלים את אותו מייל על תאריך קמפיין באירוע שאף אחד מהם לא עובד עליו.
  תאריכי קמפיין מגיעים עכשיו רק למי שמחזיק משימה באותו קמפיין, ולמנהלים
  שביקשו את התצוגה הרחבה. אחרי התיקון: 8 אנשים, 0 הודעות.
