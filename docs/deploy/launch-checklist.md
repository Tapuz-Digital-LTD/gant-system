# רשימת פעולות לפני עלייה לאוויר

מסמך מצטבר. כל דבר שדורש החלטה או פעולה אנושית נרשם כאן במקום להיזכר בו.
**שום סוד לא נכתב במסמך הזה** — רק מה לעשות ואיפה.

---

## 1. אבטחה — דחוף

- [x] ~~להחליף את סיסמת מסד הנתונים.~~ **בוצע 9.9.2026.**
      הוחלפה ב-`ALTER ROLE` דרך `server/db/rotate-password.ts`, אומתה בחיבור
      אמיתי, ועודכנה ב-`.env.local` וב-Vercel production. הסיסמה שנחשפה כבר
      אינה תקפה.

- [ ] **`AUTH_SECRET` חדש לפרודקשן** — `openssl rand -base64 32`.
      החלפה שלו מנתקת את כל המחוברים, אז עדיף לפני שיש מי להתנתק.

- [ ] לוודא ש-`.env.local` ו-`.backups/` לא נכנסו לגיט. שניהם ב-`.gitignore`,
      וכדאי לוודא שוב לפני הפריסה הראשונה.

## 2. משתני סביבה

- [ ] להזין את הרשימה מ-[vercel-env.md](vercel-env.md).
- [ ] **לא להזין `GANTT_NOTIFICATIONS_SEND`.** משתנה שלא קיים = מצב לוג.
- [ ] **לא להזין `ANTHROPIC_API_KEY`** עד שחיבור ה-AI יושלם.

## 3. מיגרציות

מסד הפרודקשן חסר שלוש טבלאות — מיגרציות 0007–0009 לא רצו שם.
בלעדיהן מסך ההגדרות והפעמון לא יעבדו.

- [ ] `DATABASE_URL_UNPOOLED=... npm run db:migrate`
- [ ] אחר כך `npm run db:check` כדי לוודא שהמסד תואם לקוד.

המיגרציות הממתינות:

| קובץ | מה הוא עושה |
|---|---|
| `0007_notifications.sql` | טבלת ההתראות |
| `0008_notification_prefs.sql` | העדפות אישיות + הגדרות ארגון |
| `0009_user_phone.sql` | מספר נייד למשתמש |
| `0010_drop_launch_date.sql` | מסיר עמודה יתומה שנוצרה ידנית בפרודקשן |

## 4. Inforu

- [ ] בפורטל: לוודא שהמסלולים `/v2/SMS/SendSms` ו-`/Umail/Message/Send` פתוחים.
- [ ] לבדוק את המכסה, ומי מקבל התראה כשהיא נגמרת.
- [ ] לוודא ש-`GANTT_SMS_SENDER` ברשימת השולחים המאושרים ושכתובת המייל מאומתת.
      שולח לא מאושר חוזר כ-HTTP 200 עם `StatusId` שלילי — "הצליח" שלא הצליח.
- [ ] בדיקה אחת לנמען שאושר בשמו, אחרי שאראה בדיוק מה עומד להישלח.
- [ ] רק אז — להחליט על `GANTT_NOTIFICATIONS_SEND=true`.

## 5. הסיכום היומי

- [ ] `CRON_SECRET` ב-Vercel.
- [ ] לקרוא את הלוג שבוע לפני שמפעילים שליחה.
- [ ] להוסיף ל-`vercel.json`:
      `"crons": [{ "path": "/api/cron/digest", "schedule": "0 * * * *" }]`

## 6. AI

- [ ] להשלים את המעבר מ-Gemini ל-Claude. **טרם בוצע.**
- [ ] רק אחריו — מפתח Anthropic, והחלטה על תקרת עלות.

## 7. בדיקות אחרונות

- [ ] כניסה אמיתית של עובד: קוד במייל מגיע, מסך הכניסה לא אומר "נכתב ללוג".
- [ ] `npm run check` ירוק.
- [ ] `npm run db:check` מול פרודקשן.
- [ ] מסך צר על מכשיר אמיתי, לא רק בדפדפן.

---

## מה כבר בוצע

- **9.9.2026 — נוקו נתוני הדוגמה מ-`neondb`.** 108 רשומות: 67 אירועים,
  37 שורות יומן, 4 לוחות. נשמרו 8 משתמשים, מטריצת ההרשאות והחיבורים.
  גיבוי מלא של כל 14 הטבלאות ב-`.backups/` לפני המחיקה.
- **9.9.2026 — אומת שהמסד אינו משותף.** כל הטבלאות שייכות למערכת הזאת.
